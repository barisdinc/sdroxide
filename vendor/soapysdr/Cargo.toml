# VENDORED, PATCHED COPY — see vendor/soapysdr/PROVENANCE.md.
#
# Upstream soapysdr 0.5.1, plus one added method (`Device::setting_info`).
# The examples and the dev-dependencies that existed only for them are dropped;
# nothing else is changed.

# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.85"
name = "soapysdr"
version = "0.5.1"
authors = ["Kevin Mehall <km@kevinmehall.net>"]
build = false
exclude = [
    "shell.nix",
    ".github",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = """
Library wrapping SoapySDR, a hardware abstraction layer for many software defined radio devices,
including rtl-sdr, HackRF, USRP, LimeSDR, BladeRF, and Airspy.
"""
readme = "README.md"
keywords = [
    "sdr",
    "radio",
]
categories = [
    "api-bindings",
    "hardware-support",
]
license = "BSL-1.0 OR Apache-2.0"
repository = "https://github.com/kevinmehall/rust-soapysdr"

[features]
default = ["log"]

[lib]
name = "soapysdr"
path = "src/lib.rs"

[dependencies.log]
version = "0.4"
optional = true

[dependencies.num-complex]
version = "0.4"

[dependencies.soapysdr-sys]
version = "0.8.0"

